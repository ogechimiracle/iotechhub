/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['iotechhub.netlify.app'], // Allow the Netlify domain (if using relative URLs)
      },
};

export default nextConfig;
